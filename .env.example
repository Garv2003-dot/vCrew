# Google Gemini API key (required for AI allocation and intent extraction).
# Get a key at https://makersuite.google.com/app/apikey
# Never commit the real key or expose it to the browser; only the backend uses it.
GEMINI_API_KEY=your_gemini_api_key_here
